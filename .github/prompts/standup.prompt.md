---
mode: agent
description: Summarise current spec activity across the team
---

Read and follow the instructions in `cortex/commands/standup.md` exactly.

Run `python3 cortex.py ls --specs` first to get current state.
Only report what the spec files actually say.
