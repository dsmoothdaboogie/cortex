---
mode: agent
description: Generate an implementation plan from a spec
---

Read and follow the instructions in `commands/build.md` exactly.

Only proceed if the spec has passed `/review` with verdict READY.
If the spec hasn't been reviewed, run `/review` on it first and report the verdict.

Pull cortex context before generating the plan as defined in `commands/build.md`.
