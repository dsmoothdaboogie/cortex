Read and follow the instructions in `cortex/commands/tools/learn.md` exactly.

Arguments: $ARGUMENTS

Parse TOPIC and SECTION from the arguments (ask the user if not provided).
Review the current conversation thread for corrections the user made to the agent's output.
Produce one structured entry per correction relevant to the given TOPIC.
Append to the appropriate knowledge file and ingest.
