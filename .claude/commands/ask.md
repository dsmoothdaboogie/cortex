Read and follow the instructions in `cortex/commands/ask.md` exactly.

Arguments: $ARGUMENTS

Never answer from assumptions. Always run the cortex query first.
If the knowledge base has no relevant content, say so clearly.
