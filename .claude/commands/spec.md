Read and follow the instructions in `cortex/commands/2-spec.md` exactly.

The user will provide a ticket number and requirement — which may be a short title or a long multi-paragraph story. Either is valid input.

Arguments: $ARGUMENTS

Before writing anything, load context as defined in `cortex/commands/2-spec.md` — read local knowledge files directly, and query linked repos if `.cortex-repos.json` is non-empty.
