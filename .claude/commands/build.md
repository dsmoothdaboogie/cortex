Read and follow the instructions in `cortex/commands/build.md` exactly.

Arguments: $ARGUMENTS

Only proceed if the spec has passed `/review` with verdict READY. If the spec hasn't been reviewed, run `/review` on it first and report the verdict.

Pull cortex context before generating the plan as defined in `cortex/commands/build.md`.
